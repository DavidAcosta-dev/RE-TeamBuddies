// Configuration with placeholders to be updated as RE reveals specifics
export const CONFIG = {
  // Placeholder tick; update when the main loop tick is confirmed from MAIN.EXE
  dt: 1 / 60,
  // Toggle simple integration (true) vs. no-op (false) until formulas are precise
  enableSimpleIntegration: true,
};
